# History

## 18:19 - Created app-store-release skill

Added the repository-scoped V1.0 orchestration skill with prepare, upload,
submit, and resume checkpoints; a CodexBar 1.3 dry run confirmed that the final
What's New copy is limited to changes after the 1.2 live boundary and rejects
the earlier cumulative 1.2-plus-1.3 draft without making App Store Connect
writes.

Retrospective check: no earlier app-store-release history exists; the initial
workflow keeps App Store Connect state authoritative, separates upload from
submission, and records interrupted-run recovery before the first live use.

## 18:27 - Tightened prose and review gates

Applied the repository writing standard to the skill headings and confirmed
the instructions use direct, concrete language. The first PR review also led
to an explicit merge-and-re-resolve checkpoint before release validation and a
declaration of the four required companion skills.

Retrospective check: release preparation now reaches synchronized `main` before
the immutable candidate SHA is validated or archived, closing a gap found by
the first automated review.

## 18:29 - Declared the timestamp runtime

Added PowerShell 7 to compatibility and made the hook command directly runnable
from the repository's macOS shell while retaining the required `Get-Date`
timestamp source.

Retrospective check: the hook now states the runtime it already used in the dry
run, so a new machine fails at dependency discovery instead of timestamp entry.

## 18:33 - Added the build-selection checkpoint

Made Submit mode select the exact processed build and save approved localized
metadata before the storefront is re-verified and marked submission-ready.

Retrospective check: Upload still stops without App Store Connect writes after
processing, while Submit now has an explicit, bounded path to a selected build.

## 18:38 - Required clean release contents

Added clean-worktree checks before validation and immediately before archiving
so the candidate SHA cannot hide modified or untracked build inputs.

Retrospective check: release evidence now binds the commit, main synchronization,
and actual filesystem contents at both validation and archive checkpoints.

## 18:43 - Bound Resume to original authority

Made the release ledger persist its authorized mode and required a new explicit
grant before Resume can expand Prepare or Upload into a write-capable stage.

Retrospective check: an interrupted dry run now stays a dry run when resumed;
the next checkpoint alone can no longer increase submission authority.

## 18:45 - Separated history from build inputs

Scoped both clean-release checks to reject every worktree change except the
inspected History entry that this skill's own hooks require.

Retrospective check: history remains recorded without changing the binary, and
any other tracked or untracked file still blocks validation and archiving.
